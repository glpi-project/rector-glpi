<?php

if (!Session::haveRight('dropdown', DELETE)) {
    // too ambiguous, it should refer to the concrete class
    return;
}

?>
-----
<?php

if (!Session::haveRight('dropdown', DELETE)) {
    // too ambiguous, it should refer to the concrete class
    return;
}

?>
