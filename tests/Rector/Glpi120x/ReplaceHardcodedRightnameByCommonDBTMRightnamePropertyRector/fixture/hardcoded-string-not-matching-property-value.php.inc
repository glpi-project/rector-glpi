<?php

// `Rack` rightname is not `rack`
if (!Session::haveRight('rack', UPDATE)) {
    return;
}

?>
-----
<?php

// `Rack` rightname is not `rack`
if (!Session::haveRight('rack', UPDATE)) {
    return;
}

?>
